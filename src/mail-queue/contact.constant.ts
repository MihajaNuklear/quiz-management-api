import config from '../config/configuration.constant';

const FRONT_BASE_URL = config().front.frontUrl;

export const LINK_TO_SUMMARY = `${FRONT_BASE_URL}/event/summary`;

export const ICON_SUMMARY_DIRECTORY = '';
